server:
  port: 80

spring:
  application:
    name: cloud-order-server

#eureka的配置
eureka:
  client:
    register-with-eureka: true
    fetchRegistry: true
    service-url:
      defaultZone: http://localhost:7001/eureka